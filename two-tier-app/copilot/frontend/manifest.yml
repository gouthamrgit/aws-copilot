name: frontend
type: Load Balanced Web Service

image:
  build:
    context: .
    dockerfile: frontend/Dockerfile
  port: 3000   # <--- Must be inside `image`

cpu: 256
memory: 512
count: 1

http:
  path: '/'
  healthcheck: '/health'

variables:
  BACKEND_URL: "http://backend:8080"

exec: true
